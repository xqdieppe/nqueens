#include <stdio.h>
#include "nqueens.h"

int main(int argc, char **argv) {
    printf("nqueens(4) = %d\n", nqueens(4));
    printf("nqueens(6) = %d\n", nqueens(6));
    printf("nqueens(7) = %d\n", nqueens(7));
    printf("nqueens(8) = %d\n", nqueens(8));
}